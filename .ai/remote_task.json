{
  "version": 1,
  "revision": 6,
  "persist": false,
  "goal": "Run a visible graphical Airi-PC task so Airi Live can be verified from a real phone.",
  "scope": [
    "."
  ],
  "limits": {
    "max_time": 600,
    "max_iterations": 12,
    "max_retries": 0,
    "max_tool_calls": 20
  },
  "steps": [
    {
      "id": "visible-wikipedia-tour",
      "title": "Open Wikipedia, search and scroll visibly",
      "operation": "test",
      "args": {
        "command": "python -m playwright install chromium && PYTHONPATH=computer python - <<'PY'\nimport json, time\nfrom control_plane.live_telemetry import live_emit, live_flush\nfrom server import browser\n\nlive_emit('runtime','Live phone test started','Opening a real graphical browser task','running',dedupe_key='phone-live-test:start')\nb = browser()\n\ndef tour():\n    page = b._ensure()\n    page.goto('https://www.wikipedia.org/', wait_until='domcontentloaded', timeout=45000)\n    live_emit('runtime','Wikipedia opened','Waiting so the phone can show the page','running',dedupe_key='phone-live-test:wikipedia')\n    page.wait_for_timeout(5000)\n\n    search = page.locator('input[name=search]').first\n    if search.count() and search.is_visible():\n        search.click()\n        search.fill('Artificial intelligence')\n        page.wait_for_timeout(2500)\n        search.press('Enter')\n        page.wait_for_load_state('domcontentloaded', timeout=45000)\n    else:\n        page.goto('https://en.wikipedia.org/wiki/Artificial_intelligence', wait_until='domcontentloaded', timeout=45000)\n\n    live_emit('runtime','Article visible','Artificial intelligence article loaded','running',dedupe_key='phone-live-test:article')\n    page.wait_for_timeout(6000)\n    page.evaluate('window.scrollTo({top: document.body.scrollHeight * 0.45, behavior: \\"smooth\\"})')\n    page.wait_for_timeout(5000)\n    live_emit('runtime','Scrolling page','Airi-PC moved through the article','running',dedupe_key='phone-live-test:scroll')\n    page.evaluate('window.scrollTo({top: 0, behavior: \\"smooth\\"})')\n    page.wait_for_timeout(5000)\n    return {'status':'done','title':page.title(),'url':page.url}\n\ntry:\n    result = b.call(tour)\n    live_emit('runtime','Live phone test completed',result.get('title','done'),'completed',dedupe_key='phone-live-test:done')\n    print(json.dumps(result, ensure_ascii=False))\nfinally:\n    live_flush(10.0)\nPY",
        "cwd": ".",
        "timeout": 180
      },
      "repository": ".",
      "workspace": ".",
      "verification": {
        "required": true
      }
    }
  ]
}

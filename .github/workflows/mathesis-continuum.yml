name: MATHESIS Omega Continuum

on:
  workflow_dispatch:
  schedule:
    - cron: '3,8,13,18,23,28,33,38,43,48,53,58 * * * *'
  push:
    branches: [main]
    paths:
      - 'computer/mathesis/**'
      - '.github/workflows/mathesis-continuum.yml'

permissions:
  actions: write
  contents: write

concurrency:
  group: mathesis-omega-continuum
  cancel-in-progress: false

jobs:
  evolve:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONPATH: computer
      MATHESIS_STATE_DIR: /tmp/mathesis-runtime-state
      STATE_REPO: /tmp/mathesis-state-repo
      MATHESIS_CHALLENGERS: '3'
      MATHESIS_STUDY_EVERY: '6'
      MATHESIS_MIN_CHAIN_SECONDS: '300'
    steps:
      - name: Mark autonomous cycle start
        run: echo "MATHESIS_CHAIN_STARTED_EPOCH=$(date -u +%s)" >> "$GITHUB_ENV"

      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install mathematical runtime
        run: |
          python -m pip install --disable-pip-version-check -r computer/mathesis/requirements.txt

      - name: Cache Lean toolchain
        uses: actions/cache@v4
        with:
          path: ~/.elan
          key: mathesis-lean-${{ runner.os }}-v4.34.0

      - name: Install stable Lean 4 verifier
        run: |
          set -euo pipefail
          if [ ! -x "$HOME/.elan/bin/lean" ]; then
            curl --proto '=https' --tlsv1.2 -sSf \
              https://raw.githubusercontent.com/leanprover/elan/master/elan-init.sh \
              -o /tmp/elan-init.sh
            sh /tmp/elan-init.sh -y --default-toolchain leanprover/lean4:v4.34.0
          fi
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          "$HOME/.elan/bin/elan" default leanprover/lean4:v4.34.0
          "$HOME/.elan/bin/lean" --version

      - name: Restore or initialize persistent MATHESIS state branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git clone --quiet \
            "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" \
            "$STATE_REPO"
          cd "$STATE_REPO"
          git config user.name "MATHESIS Omega"
          git config user.email "mathesis-omega@users.noreply.github.com"

          if git ls-remote --exit-code --heads origin mathesis-state >/dev/null 2>&1; then
            git fetch --quiet origin mathesis-state:mathesis-state
            git checkout --quiet mathesis-state
          else
            git checkout --orphan mathesis-state
            git rm -rf . >/dev/null 2>&1 || true
            mkdir -p mathesis-state
            printf '%s\n' 'Persistent proof-gated MATHESIS model state.' > mathesis-state/README.txt
            git add mathesis-state/README.txt
            git commit -m "mathesis-state: initialize"
            git push --quiet origin HEAD:mathesis-state
          fi

          mkdir -p "$MATHESIS_STATE_DIR"
          if [ -d "$STATE_REPO/mathesis-state" ]; then
            cp -a "$STATE_REPO/mathesis-state/." "$MATHESIS_STATE_DIR/" || true
          fi

      - name: Normalize bounded MATHESIS audit history
        run: |
          set -euo pipefail
          python -m mathesis.history --state-dir "$MATHESIS_STATE_DIR"
          test -f "$MATHESIS_STATE_DIR/history.jsonl"
          test -f "$MATHESIS_STATE_DIR/history-manifest.json"

      - name: Run bounded self-evolution cycle
        run: |
          set -euo pipefail
          python -m mathesis.evolution_cycle | tee "$MATHESIS_STATE_DIR/last-cycle.json"
          python -m mathesis.cli --status | tee "$MATHESIS_STATE_DIR/status.json"
          python -m mathesis.health
          test -f "$MATHESIS_STATE_DIR/champion.json"
          test -f "$MATHESIS_STATE_DIR/router.json"
          test -f "$MATHESIS_STATE_DIR/champion_model.py"
          test -f "$MATHESIS_STATE_DIR/discoveries.json"
          test -f "$MATHESIS_STATE_DIR/discovery-history.jsonl"
          test -f "$MATHESIS_STATE_DIR/history.jsonl"
          test -f "$MATHESIS_STATE_DIR/history-manifest.json"
          test -f "$MATHESIS_STATE_DIR/health.json"

      - name: Persist and verify state heartbeat
        id: persist
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          cd "$STATE_REPO"
          rm -rf mathesis-state
          mkdir -p mathesis-state
          cp -a "$MATHESIS_STATE_DIR/." mathesis-state/
          git add -A mathesis-state
          git commit --allow-empty -m "mathesis-state: sync $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push --quiet origin HEAD:mathesis-state

          local_sha="$(git rev-parse HEAD)"
          remote_sha="$(git ls-remote origin refs/heads/mathesis-state | awk '{print $1}')"
          test -n "$remote_sha"
          test "$local_sha" = "$remote_sha"
          echo "MATHESIS state persisted at $remote_sha"
          echo "persisted=true" >> "$GITHUB_OUTPUT"

      - name: Hand off to the next autonomous cycle
        if: success() && steps.persist.outputs.persisted == 'true' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          now="$(date -u +%s)"
          start="${MATHESIS_CHAIN_STARTED_EPOCH:-$now}"
          elapsed="$((now - start))"
          remaining="$((MATHESIS_MIN_CHAIN_SECONDS - elapsed))"
          if (( remaining > 0 )); then
            echo "Waiting ${remaining}s so autonomous cycles stay near the supported five-minute cadence."
            sleep "$remaining"
          fi

          api_base="https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/mathesis-continuum.yml/runs"
          curl_common=(--fail-with-body -sS --connect-timeout 10 --max-time 30 --retry 3 --retry-delay 2 --retry-all-errors)
          if payload="$(curl "${curl_common[@]}" \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "${api_base}?per_page=50")"; then
            active_other="$(jq --argjson current "${GITHUB_RUN_ID}" '
              [
                .workflow_runs[]
                | select(.id != $current)
                | select(
                    .status == "pending"
                    or .status == "queued"
                    or .status == "in_progress"
                    or .status == "waiting"
                    or .status == "requested"
                  )
              ] | length
            ' <<<"$payload")"
          else
            echo "Could not inspect active runs after bounded retries; attempting one continuity dispatch."
            active_other=0
          fi

          if (( active_other > 0 )); then
            echo "Another MATHESIS continuum run is already queued/running; no duplicate handoff."
            exit 0
          fi

          curl "${curl_common[@]}" -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/mathesis-continuum.yml/dispatches" \
            -d '{"ref":"main"}'

          echo "Next autonomous MATHESIS cycle dispatched."
